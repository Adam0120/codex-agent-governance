interface:
  display_name: "Govern Agent System"
  short_description: "Automatic bounded delegation through eight fixed roles"
  default_prompt: "Load $govern-agent-system before spawning or reusing custom agents, then choose and brief the smallest useful agent only when delegation materially helps."
