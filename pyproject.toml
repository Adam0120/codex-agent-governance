[project]
name = "codex-agent-governance"
version = "0.1.1"
description = "Experimental portable governance control plane for Codex agents"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}

[tool.unittest]
start-directory = "tests"
